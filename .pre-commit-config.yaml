repos:
  -   repo: https://github.com/llvm/llvm-project
      rev: release/15.x
      hooks:
        - id: clang-format
          args: [--style=file]
        - id: clang-tidy
          args: [--config-file=.clang-tidy]
