{
  "name": "aos-ui",
  "version": "0.1.0",
  "description": "Web UI for AOS",
  "scripts": {
    "build": "parcel build index.html --dist-dir dist",
    "lint": "eslint *.js"
  },
  "devDependencies": {
    "parcel": "^2.8.3",
    "eslint": "^9.28.0"
  }
}
